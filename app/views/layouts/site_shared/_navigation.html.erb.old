<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
  <a class="navbar-brand" href="<%= site_home_index_path %>"><i class="fa fa-user-md fa-2x" aria-hidden="true"></i></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarsExampleDefault">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in?%>
        <li class="nav-item active">
          <a class="nav-link" href="#">Agenda<span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item active">
          <a class="nav-link" href="#">Tratamentos<span class="sr-only">(current)</span></a>
        </li>
        <li class="nav-item dropdown active">
          <% if policy(Customer).index? || policy(Customer).index? %>         
            <a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Cadastros</a>
            <div class="dropdown-menu" aria-labelledby="dropdown01">
              <% if policy(Customer).index? %>
                <%= link_to site_customers_path,{:class =>"dropdown-item"} do %>
                  <%= t 'navigation.customers' %>
                <% end %>
              <% end %>
              <% if policy(Customer).index? %>  
                <%= link_to site_suppliers_path ,{:class =>"dropdown-item"} do %>
                  <%= t 'navigation.suppliers' %>
                <% end %>
              <% end %>
              <%= link_to site_professionals_path,{:class =>"dropdown-item"} do %>
                <%= t 'navigation.professionals' %>
              <% end %>
            </div>
          <% end %>
        </li>
        <li class="nav-item dropdown active">
          <% if policy(Supplier).index? || policy(Supplier).index? %>         
            <a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Receber</a>
            <div class="dropdown-menu" aria-labelledby="dropdown01">
              <% if policy(Supplier).index? %>
                <%= link_to site_receivable_categories_path ,{:class =>"dropdown-item"} do %>
                  <%= t 'navigation.receivable_categories' %>
                <% end %>
              <% end %>
              <% if policy(Supplier).index? %>
                <%= link_to site_receivables_path ,{:class =>"dropdown-item"} do %>
                  <%= t 'navigation.receivables' %>
                <% end %>
              <% end %>
            </div>
          <% end %>
        <li class="nav-item dropdown active">
          <a class="nav-link dropdown-toggle" href="#" id="dropdown01" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Pagar</a>
          <div class="dropdown-menu" aria-labelledby="dropdown01">
            <%= link_to site_payable_categories_path ,{:class =>"dropdown-item"} do %>
              <%= t 'navigation.payable_categories' %>
            <% end %>
            <%= link_to site_payables_path ,{:class =>"dropdown-item"} do %>
              <%= t 'navigation.payables' %>
            <% end %>
          </div>
        </li>
        <% end %>
      </ul>
    <div class="nav nav-bar pull-right">
      <% if user_signed_in?%>
        <form class="form-inline my-2 my-lg-0">
            <%= link_to destroy_user_session_path, style:"margin-top:-5px", method: :delete do%>
			        <i class="fa fa-sign-out fa-2x" style="color:white" aria-hidden="true"></i> 
		        <% end %>  
        </form>
      <% end %>
    </div>
  </div>
</nav>
