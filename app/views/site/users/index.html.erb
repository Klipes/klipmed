<%= render :partial => 'shared/top', 
  :locals => { :icon => 'fa fa-list-ul fa-2x', :model => User,
               :new_path => new_site_user_path, :new_path_hint => 'Novo usuário' } %>    

</p>

<div class="col-sm-12">
  <div class="row">
    <div class="col-sm-12">
      <table class="table table-sm table-hover table-sm">
          <thead>
              <tr class="row">
                  <th class="col-sm-9"><%= User.human_attribute_name("email") %></th>
                  <th class="col-sm-2"><%= User.human_attribute_name("user_type") %></th>
                  <th class="col-sm-1"></th>
              </tr>
          </thead>
          <tbody>
            <% @users.each do |user| %>
              <tr class="row">
                <td class="col-sm-9"><%= user.email %></td> 
                <td class="col-sm-2"><%= user.user_type_i18n %></td> 
                <td class="col-sm-1">
                  <div class="pull-right">
                    <%= link_to edit_site_user_path(user) ,class: "btn btn-sm btn-default" do%>
                      <i class="fa fa-edit"></i>
                    <% end %> 
                    <%= link_to root_path ,class: "btn btn-sm btn-default" do%>
                      <i class="fa fa-trash-o"></i>
                    <% end %>             
                  </div>   
                </td>    
              </tr>
            <% end %>  
          </tbody>
      </table>
    </div>
  </div>
</div>

<%= paginate @users %>
