<%= render :partial => 'shared/top', 
  :locals => { :icon => 'fa fa-user fa-2x', :model => Customer,
               :new_path => new_site_customer_path, :new_path_hint => 'Nova Empresa' } %>       

</p>

<%= form_tag site_customers_path(format: :js), method: :get, remote: true do %>
  <div class="form-group row">
    <div class="col-sm-11">
      <%= text_field_tag 'search_text', '', class: "form-control" %>
    </div>
    <div class="col-sm-1">
      <%= button_tag :class => "btn btn-primary float-right" do %>
        <i class="fa fa-search"></i>
      <% end %>
    </div>
  </div>
<% end %> 

</p>

<div id ="table_customers" class="col-sm-12">
  <%= render :partial => 'customers' %>
</div>

<%= paginate @customers %>
