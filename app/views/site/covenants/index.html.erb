<%= render :partial => 'shared/top', 
  :locals => { :icon => 'fa fa-hospital-o fa-2x', :title => Covenant.model_name.human,
               :new_path => new_site_covenant_path, :new_path_hint => 'Novo Convênio' } %>    

</p>

<div class="col-sm-12">
  <div class="row">
    <div class="col-sm-12">
      <table class="table table-sm table-hover table-sm">
          <thead>
              <tr class="row">
                  <th class="col-sm-11"><%= Covenant.human_attribute_name("description") %></th>
                  <th class="col-sm-1"></th>
              </tr>
          </thead>
          <tbody>
            <% @covenants.each do |covenant| %>
              <tr class="row">
                <td class="col-sm-11"><%= covenant.description %></td> 
                <td class="col-sm-1">
                  <div class="pull-right">
                    <%= link_to edit_site_covenant_path(covenant) ,class: "btn btn-sm btn-default" do%>
                      <i class="fa fa-edit"></i>
                    <% end %> 
                    <%= link_to root_path ,class: "btn btn-sm btn-default" do%>
                      <i class="fa fa-trash-o"></i>
                    <% end %>             
                  </div>   
                </td>    
              </tr>
            <% end %>  
          </tbody>
      </table>
    </div>
  </div>
</div>

<%= paginate @covenants %>

